-- =============================================================================
-- 기존 프로젝트용: actors 테이블에 맡은 배역(role_name) 컬럼 추가
-- =============================================================================
-- 이미 schema.sql 로 DB 를 만든 뒤라면, Supabase SQL Editor 에서 이 스크립트만 실행하세요.
-- (처음부터 새로 만들 때는 갱신된 schema.sql 전체를 쓰면 이 마이그레이션은 생략 가능)
-- =============================================================================

alter table public.actors
  add column if not exists role_name text;

comment on column public.actors.role_name is '공연에서 맡은 배역(캐릭터명 등)';
